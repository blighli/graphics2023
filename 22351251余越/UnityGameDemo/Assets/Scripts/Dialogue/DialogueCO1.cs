using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DialogueCO1 : MonoBehaviour
{
    //public GameObject dialogueUI;

    void Start()
    {
        
    }

    void Update()
    {
        //Collider[] hitColliders = Physics.OverlapSphere(transform.position, 5);
        //for (int i = 0; i < hitColliders.Length; i++)
        //{
        //    if (!dialogueUI.GetComponent<DialogueUI>().dialogueDatas[0].ifShown
        //    && hitColliders[i].gameObject.CompareTag("Player"))
        //    {
        //        dialogueUI.GetComponent<DialogueUI>().dialogueDatas[0].ifShown = true;
        //        dialogueUI.GetComponent<DialogueUI>().startShowDialogue(0);
        //        return;
        //    }
        //}
    }
}
